Future<void> saveFile(List<int> bytes, String fileName) async {
  // Web download implementation intentionally disabled in shared layer to
  // avoid web-only imports here. Keep no-op behavior predictable.
  return;
}
